environment: stage

deployment:
  image:
    tag: backstage-minikube-lab
  replicas: 1

  labels:
    environment: stage
    app: my-app-backend-stage

  env:
    redis_host: my-app-redis-stage-master 
