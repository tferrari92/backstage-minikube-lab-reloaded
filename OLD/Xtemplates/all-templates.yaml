apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: all-templates
  description: A collection of all Backstage software templates
spec:
  targets:
    - ./new-nodejs/template.yaml
    - ./new-nodejs-in-existing-repo/template.yaml
    - ./new-documentation/template.yaml
